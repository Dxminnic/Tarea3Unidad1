import os
from psycopg.row import dict_row
from dotenv import load_dotenv

#carga las variables del"

load_dotenv()

DB_HOST = os.get_env("DB_HOST")
DB_PORT = os.get_env("DB_HOST")
DB_ = os.get_env("DB_HOST")
DB_HOST = os.get_env("DB_HOST")
DB_HOST = os.get_env("DB_HOST")